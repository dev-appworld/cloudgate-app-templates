{
  "_comment": "Reference copy of the runtime config Cloudgate injects for this app. The React app reads these from its .env (Vite import.meta.env) at build/runtime; this file documents the mapping with {{placeholders}} resolved per workspace. The app does not read this file directly.",
  "VITE_APP_NAME": "Cloudgate CRM",
  "VITE_IDP_BASE_URL": "{{hubUrl}}",
  "VITE_IDP_API_URL": "{{apiUrl}}",
  "VITE_IDP_TENANCY_NAME": "{{tenancyName}}",
  "VITE_IDP_RETURN_URL": "{{devPreviewUrl}}",
  "VITE_CLOUDGATE_API_URL": "{{workflowGatewayUrl}}/sbx/crm",
  "VITE_API_KEY": "{{apiKey}}",
  "VITE_API_SECRET": "{{apiSecret}}",
  "environment": "sbx",
  "workflowKeys": {
    "_comment": "Set these as project/global keys in Cloudgate for live email (SendGrid). Not stored in the browser.",
    "SENDGRID_API_KEY": "{{sendgridApiKey}}",
    "SENDGRID_FROM_EMAIL": "{{sendgridFromEmail}}"
  }
}
